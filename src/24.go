package main

import (
    "fmt"
)

func main() {
    // Define a variable to hold the sum
    var sum int

    // Initialize the sum to 0
    sum = 0

    // Example calculation
    sum += 3
    sum += 4
    sum += 5
}
